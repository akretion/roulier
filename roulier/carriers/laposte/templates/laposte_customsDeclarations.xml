<customsDeclarations>
	<includeCustomsDeclarations>{{ (customs|length > 0) | lower }}</includeCustomsDeclarations>
	{% if (customs|length > 0) %}
	<contents>
		{% for article in customs %}
		<article>
			<description>{{article.description}}</description>
			<quantity>{{article.quantity}}</quantity>
			<weight>{{article.weight}}</weight>
			<value>{{article.value}}</value>
			<hsCode>{{article.hs}}</hsCode>
			<originCountry>{{article.originCountry}}</originCountry>
		</article>
		<category>
			<value>{{ article.category }}</value>
		</category>
		{% endfor %}
	</contents>
	{% endif %}
</customsDeclarations>
